import styled from 'styled-components';

export const PictureBox = styled.div`
  display: flex;
  flex-direction: column;
  text-align: center;
  color: var(--title);

  p {
    margin-top: 0.2rem;
    font-size: 0.8rem;
    opacity: 0.9;
  }
`;
